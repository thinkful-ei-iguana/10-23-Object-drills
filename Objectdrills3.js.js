const loaf = {
    flour: 300,
    water: 210,
    hydration: function(){
         let result = (loaf.water / loaf.flour) * 100;
         return result;
    }
}
let finalResult = loaf.hydration();
console.log(finalResult);

for(let property in newObject)

let hero = data.map(function(data){ 

{ ${data.name} reports to ${data.boss} as ${data.jobTitle};
} `${data.jobTitle} ${data.name} doesn't report to anybody.` } }); return hero;

let superHeroes = [ 
    { name: 'Clark Kent', jobTitle:'Reporter', boss: "Bruce Wayne" },
    { name: 'Bruce Wayne', jobTitle:'Designated BoneBreaker' }, 
    { name: "Sonic the Hedgehog", jobTitle:"GO FAST!", boss: "Bruce Wayne" }, 
];


let objKeyLength = Object.keys(superHeroes);
if(Object.keys(objKeyLength).length !== 3){
    return `${data.jobTitle} ${data.name} doesn't report to anybody.`
}